<% provide :title, @user.name %>
<% content_for :head do %>
  <meta name="description" content="<%= @user.name %>のモノページ。Hanakoの欲しいモノ、持ってるモノを表示しています。">
<% end %>
<%= render 'profile' , user: @user %>


<%= render 'items/list' , items: @items %>